console.log("Números pares de 1 a 100:");

// Loop de 1 a 100
for (let i = 1; i <= 100; i++) {
    // Verifica se o número é par
    if (i % 2 === 0) {
        console.log(i); // Imprime o número par
    }
}

/*
#include <stdio.h>

int main() {
    int a, b;

    // Pergunta e lê o primeiro valor inteiro
    printf("");
    scanf("%d", &a);

    // Pergunta e lê o segundo valor inteiro
    printf("");
    scanf("%d", &b);

    // Calcula a soma dos dois valores
    int x = a + b;

    // Imprime o resultado conforme o formato especificado
    printf("X = %d\n", x);

    return 0;
}

/*